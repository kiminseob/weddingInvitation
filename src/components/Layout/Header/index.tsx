import { Typography } from "@mui/material";
import * as S from "./styled";

const Header = () => {
  return (
    <S.Header>
      <Typography variant="h6">2023. 4. 14</Typography>
      <Typography variant="body1">일요일</Typography>
    </S.Header>
  );
};

export default Header;
